########################################
# Required
########################################
OPENAI_API_KEY=sk-...
METRICS_PASSWORD=change-me
METRICS_AUTH_SECRET=your-strong-secret

########################################
# AI settings (optional)
########################################
# OPENAI_MODEL=gpt-5-nano
# OPENAI_SERVICE_TIER=flex   # flex | auto | priority (default: flex; auto maps to standard)
# OPENAI_REASONING_EFFORT=low  # low | medium | high
# OPENAI_VERBOSITY=low            # low | medium | high

########################################
# Logging (optional)
########################################
# LOG_LEVEL=info              # debug | info | warn | error
# LOG_MAX_LINES=5000          # max kept in store per key
# LOG_KEY=atsbuddy:logs       # list key for logs

########################################
# Rate limiting for /api/analyze (optional)
########################################
# RATE_WINDOW_SECONDS=60
# RATE_MAX=20

########################################
# Storage backend (optional, minimal config)
########################################
# STORAGE_BACKEND=file        # file | (omit for auto: Upstash if env present, else file fallback)
# DATA_DIR=.data/atsbuddy     # base dir for file storage; on Vercel defaults to /tmp/atsbuddy-data

########################################
# Optional: Vercel KV / Upstash Redis(auto-detected if set)
########################################
# UPSTASH_REDIS_REST_URL=https://us1-your-db.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-token

